---
title: "Freeze"
engine: pari-gp
format: html
freeze: true
---

```{gp}
\\ gp starts from the same seed every time, so a re-execution would print
\\ the same number and the test could not fail. Seed from the clock instead.
setrand(getwalltime()); random(10^18)
```
